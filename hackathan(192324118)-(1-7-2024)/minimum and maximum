Arr[]=[2,4,6,8,10,12,14,18]
           min = arr[0]
max = arr[0]
for num in arr:
    if num < min:
        min = num
    if num > max:
        max = num
print("Minimum number:",min)
print("Maximum number:", max)
